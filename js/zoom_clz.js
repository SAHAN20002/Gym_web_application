document.getElementById('membershipForm').addEventListener('submit', function(e) {
    e.preventDefault();
    // Add logic to handle form submission
    alert("Zoom class information updated.");
});

function goBack() {
    window.history.back();
}
